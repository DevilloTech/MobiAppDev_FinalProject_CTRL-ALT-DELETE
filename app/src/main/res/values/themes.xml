<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.TechnicalSumm2"
        parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary -->
        <item name="colorPrimary">@color/primaryBrown</item>
        <item name="colorPrimaryVariant">@color/primaryBrown</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary -->
        <item name="colorSecondary">@color/primaryBrown</item>
        <item name="colorSecondaryVariant">@color/primaryBrown</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Window -->
        <item name="android:windowBackground">@color/background</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/primaryBrown</item>

    </style>
    <style name="Theme.TechnicalSumm2.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>

</resources>